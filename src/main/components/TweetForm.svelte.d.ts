import { Store } from 'svelte/store'

export declare class TweetForm extends Twiterian.Svelte {
  constructor(options: {
    target: Element | HTMLElement | null,
    data?: any,
    store?: Store,
  })
}

export default TweetForm
